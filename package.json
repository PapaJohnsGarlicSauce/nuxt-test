{
  "name": "nuxtjs-site",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.15.8"
  },
  "devDependencies": {
    "node-sass": "^4.13.1",
    "push-dir": "^0.4.1",
    "sass-loader": "^8.0.2",
    "vue-loader": "^15.9.1",
    "vue-template-compiler": "^2.6.11"
  }
}
